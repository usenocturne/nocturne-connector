{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the Nocturne Connector tray host",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
